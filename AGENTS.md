# MoonBit Project

## Commands

```bash
just check
just test
just fmt
just info
```

## Notes

- Keep OpenUI Lang parsing and validation in `src/core` and `src/schema`.
- Keep Rabbita-specific conversion in `src/rabbita`.
- Add focused snapshot tests when parser output or diagnostics change.
